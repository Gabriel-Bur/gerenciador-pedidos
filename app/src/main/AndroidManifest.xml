<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infnet.android.gerenciador_pedidos">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PopUp_Files.ObsPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".PopUp_Files.MesaPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".EncerrarActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MesaActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".PedidoActivity" />
        <activity android:name=".CardapioActivity"></activity>
        <activity android:name=".FormularioActivity"></activity>
    </application>

</manifest>